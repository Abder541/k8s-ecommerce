# ConfigMap pour les parametres non-sensibles du backend
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ecommerce
data:
  MONGO_HOST: "127.0.0.1"      # Pod 2 = backend + mongo dans le meme Pod -> localhost
  MONGO_PORT: "27017"
  PORT: "3000"
  NODE_ENV: "production"
